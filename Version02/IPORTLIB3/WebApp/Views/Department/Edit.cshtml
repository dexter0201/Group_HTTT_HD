@using WebApp.Resources
@model DTO.Department
@{
    ViewBag.Title = Language.Edit;
}
<h2>@Language.Edit</h2>
<form method="post" action="/Department/Edit" class="frm">
    @Html.ValidationSummary(true)
    @Html.HiddenFor(model => model.DepartmentId)
    <p>
        <label>@Language.DepartmentName</label>
        @Html.TextBoxFor(model => model.DepartmentName, new { @class = "txtlarge" })
		<span class="required">*</span>   
        @Html.ValidationMessageFor(model => model.DepartmentName)
    </p>

    <p>
        <label>&nbsp;</label>
        <input type="submit" value="@Language.Save" class="btn" />
    </p>
</form>
<div>
    @Html.ActionLink(Language.BackToList, "Index")
</div>
