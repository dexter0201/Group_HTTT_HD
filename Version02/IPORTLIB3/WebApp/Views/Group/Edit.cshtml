@using WebApp.Resources
@model DTO.Group
@{
    ViewBag.Title = Language.Edit;
}
<h2>@Language.Edit</h2>
<form method="post" action="/Group/Edit" class="frm">
    @Html.ValidationSummary(true)
    @Html.HiddenFor(model => model.GroupId)
    <p>
        <label>@Language.GroupName</label>
        @Html.TextBoxFor(model => model.GroupName, new { @class = "txtlarge" })
		<span class="required">*</span>   
        @Html.ValidationMessageFor(model => model.GroupName)
    </p>

    <p>
        <label>&nbsp;</label>
        <input type="submit" value="@Language.Save" class="btn" />
    </p>
</form>
<div>
    @Html.ActionLink(Language.BackToList, "Index")
</div>
