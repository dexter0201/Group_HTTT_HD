@using WebApp.Resources
@model DTO.Major
@{
    ViewBag.Title = Language.Create;
}
<h2>@Language.Create</h2>
<form method="post" action="/Major/Create" class="frm">
    @Html.ValidationSummary(true)
    <p>
        <label>@Language.MajorName</label>
        @Html.TextBoxFor(model => model.MajorName, new { @class = "txtlarge" })
		<span class="required">*</span>   
        @Html.ValidationMessageFor(model => model.MajorName)
    </p>

	<p>
        <label>&nbsp;</label>
        <input type="submit" value="@Language.Save" class="btn" />
    </p>
</form>
<div>
    @Html.ActionLink(Language.BackToList, "Index")
</div>
