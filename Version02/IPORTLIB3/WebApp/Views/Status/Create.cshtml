@using WebApp.Resources
@model DTO.Status
@{
    ViewBag.Title = Language.Create;
}
<h2>@Language.Create</h2>
<form method="post" action="/Status/Create" class="frm">
    @Html.ValidationSummary(true)
    <p>
        <label>@Language.StatusName</label>
        @Html.TextBoxFor(model => model.StatusName, new { @class = "txtlarge" })
		<span class="required">*</span>   
        @Html.ValidationMessageFor(model => model.StatusName)
    </p>

	<p>
        <label>&nbsp;</label>
        <input type="submit" value="@Language.Save" class="btn" />
    </p>
</form>
<div>
    @Html.ActionLink(Language.BackToList, "Index")
</div>
